logging:
  level:
    root: INFO
    com.shahed.taskmanagementsystem: INFO

  appenders:
    console:
      type: Console
      encoder:
        pattern: "%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n" # Log message format